export type Groups =
  | {
      mult: string;
      divide: string;
    }
  | {
      plus: string;
      minus: string;
    };

export const REG_EXPS = {
  MULT: "(?<mult>([0-9]+)([.,]?)([0-9]*)\\*([0-9]+)([.,]?)([0-9]*))|(?<divide>([0-9]+)([.,]?)([0-9]*)\\/([0-9]+)([.,]?)([0-9]*))",
  SUM: "(?<plus>([0-9]+)([.,]?)([0-9]*)\\+([0-9]+)([.,]?)([0-9]*))|(?<minus>([0-9]+)([.,]?)([0-9]*)\\-([0-9]+)([.,]?)([0-9]*))",
};
